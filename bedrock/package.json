{
  "name": "gentlemobs-bedrock",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test test/*.test.mjs",
    "build": "npm run check && npm test && node tools/build.mjs",
    "deploy": "node tools/deploy.mjs"
  },
  "devDependencies": {
    "@minecraft/server": "2.0.0",
    "esbuild": "0.28.2",
    "fflate": "0.8.3",
    "jsonc-parser": "3.3.1",
    "typescript": "7.0.2"
  }
}
