name: GentleMobs
version: '${version}'

main: io.github.drcas.gentlemobs.GentleMobsPlugin
api-version: '26.2'
load: POSTWORLD

commands:
  gentlemobs:
    description: Main GentleMobs command
    usage: /gentlemobs